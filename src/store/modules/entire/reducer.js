// import {ADD_SUM} from "./contants"

const initialState = {
  names: "kobe"
}

const reducer = function(state = initialState, action) {
  switch(action.type) {
    // case ADD_SUM:
    //   return {...state, }
    default: 
    return state
  }
}

export default reducer